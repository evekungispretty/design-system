### Card

| Property | Kid | Adult |
| --- | --- | --- |
| Fill | `#F3F0EB` (bg/surface-2) | `#FFFFFF` (bg/page) |
| Border | 2px `#EF857D` (brand/red/500) | 2px `#4F518C` (brand/purple/500) |
| Radius | `radius/xl` (20px) | `radius/xl` (20px) |

### Layout

The thumbnail is 151px tall and runs edge to edge. Below it the body has `space/m` horizontal and `space/s` vertical padding, with `space/2xs` between the text block and the pill row. All text is centered.

### Text

| Element | Style |
| --- | --- |
| Title | `text/body-l-bold` — step 0, weight 800, 30px line height, `#1A1A1A` |
| Description | `text/caption` — step -2, weight 400, 26px line height, `#4A4A4A` |
| Caption | `text/body-sm-bold` — step -1, weight 700, `#1A1A1A` |

### Pills

The tags use the [Pill](/components/pill) fill colors: `#CE574E` for kid, `#4F518C` for adult, with white text. In this card they are set at step 0 bold (38px tall), larger than the default Pill.
