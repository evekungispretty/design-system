### Card

| Property | Value |
| --- | --- |
| Radius | `radius/lg` (16px) |
| Padding | `space/s` vertical, `space/2xs` horizontal |
| Gap | `space/3xs` (4px) |
| Border | 1px |

### Types

| Type | Fill | Border | Icon disc | Title color |
| --- | --- | --- | --- | --- |
| Video | `#FFFFFF` | `#E8DCCF` (primary/300) | `#EAF3FB` (brand/blue/300) | `#1A1A1A` |
| Live class | `#FFFCE5` (brand/yellow/300) | `#C6504E` (brand/red/900) | `#FFFFFF` | `#D5854D` (brand/orange/700) |
| Feedback | `#EAF3FB` (brand/blue/300) | `#6AA0CC` (brand/blue/700) | `#FFFFFF` | `#447CA2` (brand/blue/900) |

### Content

The icon sits in a 46px circle. Weekday is `text/body-sm-bold` (step -1); title is `text/body-bold` (step 0); subtitle and duration are `text/caption-b` (step -2). A 40px divider in `#E8DCCF` separates the header from the 44px footer.

### Footer

The link is a pill with `space/xs` and `space/2xs` padding, `elevation/1`-style shadow, and a 24px play icon. Hover fills it with `bg/surface-hover` (`#FAF8F4`). Duration text is `#B3B3B3` (text/disabled).

### Reviewed pill

A red `#CE574E` Pill-style label — step 0 bold white, 38px tall — centered on the top edge, 24px above it.
