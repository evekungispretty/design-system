### Card

| Property | Value |
| --- | --- |
| Radius | `radius/xl` (20px) |
| Fill | `#FFFFFF` (bg/page) |
| Border | 1px `#D7C6B3` (border/brand) |
| Padding | `space/m` (24px) |
| Gap between sections | `space/s` (16px) |

### Header

A 52.85px circular avatar, 14px gap, then two lines of `text/caption-b` and `text/body-bold`: the position (step -2, `#4A4A4A`) above the name (step 0, `#1A1A1A`).

### Quote

`text/body-sm` — step -1, weight 400, 26px line height, `#1A1A1A`.

### Link

Same type as the quote, colored `#447CA2` (text/link). Underlines on hover.
