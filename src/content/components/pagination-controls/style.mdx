### Arrows

40 × 40px circles (`radius/pill`), fill `#FFFFFF`, 1px border `#D7C6B3` (border/brand). The chevron is a 23px glyph in `#4A4A4A` (text/secondary). Gap between arrows and the indicator: 16px.

### Dots

10px circles, 9px apart. Current: `#4A4A4A` (neutral/500). Other dots: `#B3B3B3` (neutral/300).

### Numbers

Each number sits in a 32px circle, 8px apart, in `text/body-sm` (step -1). The current page is bold white on `#C6504E` (action/primary-active); others are `#4A4A4A` regular.

### Added in code

Arrows are disabled at the first and last page — 40% opacity (`opacity/disabled`), no hover. Figma doesn't draw a disabled state. Numbers get a light hover fill and everything gets a 2px `border/focus` focus ring.
