### Row

| Property | Value |
| --- | --- |
| Padding | `space/3xs` (4px) vertical, `space/2xs` (8px) horizontal |
| Gap (text, icon) | `space/xs` (12px) |
| Divider | 1px bottom border, `#F3F0EB` (divider/divider-light) |
| Text | 16px regular, `#1A1A1A` (text/primary) |

### Icon

`external-link`, 16 × 16px, at the right edge and vertically centered against the text.

### Added in code

A hover fill of `bg/surface-hover` (`#FAF8F4`) and a 2px `border/focus` focus ring, inset. Figma draws only the default state.
