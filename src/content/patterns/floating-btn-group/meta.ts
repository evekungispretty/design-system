import type { PatternMeta } from "@/lib/pattern-meta";

const meta: PatternMeta = {
  title: "Floating Button Group",
  description:
    "A persistent floating call-to-action stack: a right-anchored, hover-expanding stack of icon buttons on desktop, and a full-width bottom tab bar on mobile.",
  screenshot: "/patterns/floating-btn-group.png",
  screenshotWidth: 179,
  screenshotHeight: 331,
};

export default meta;
