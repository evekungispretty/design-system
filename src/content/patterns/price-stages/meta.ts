import type { PatternMeta } from "@/lib/pattern-meta";

const meta: PatternMeta = {
  title: "Price Stages",
  description:
    "A modal body that breaks a multi-stage program's price into one card per stage, each listing what's included and a stage total.",
  screenshot: "/patterns/price-stages.png",
  screenshotWidth: 1600,
  screenshotHeight: 1007,
};

export default meta;
